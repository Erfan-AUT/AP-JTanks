package game.template.logic.objectstates;

import game.template.logic.cellfillers.ComputerTank;

public class ComputerTankState extends TankState {
    public ComputerTankState(int y, int x, ComputerTank source) {
        super(y, x, source);
    }
}
